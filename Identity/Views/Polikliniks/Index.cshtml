@model IEnumerable<Identity.Models.Poliklinik>

@{
    ViewData["Title"] = "Poliklinikler";
}

<a class="btn btn-primary" asp-action="Create">Poliklinik Oluştur</a>
<hr>

<table class="table table-sm table-bordered">
	<thead>
		<tr>
			<th>Id</th>
			<th>Poliklinik Adı</th>
			<th style="width: 120px"></th>
		</tr>
	</thead>
	<tbody>

		@if(Model.Count() == 0)
		{
			<tr><td colspan="4" class="text-center">User Yok</td></tr>
		}
		@foreach (var item in Model)
		{
			<tr>
				<td>@item.PoliklinikId</td>
				<td>@item.PoliklinikAd</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.PoliklinikId" class="btn btn-primary btn-sm">Düzenle</a>
					<form asp-action="Delete" method="post" onsubmit="return confirm('emin misiniz')" style="display: inline-block;">
						<input type="hidden" value="@item.PoliklinikId" name="Id">
						<button type="submit" class="btn btn-danger btn-sm">Sil</Button>
					</form>
				</td>
			</tr>
		}
	</tbody>
</table>